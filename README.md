# tetros
tetris but as a bootloader!

i got bored so i made this in the evening on call

assemble with fasm e.g. ```fasm tetros.asm tetros.bin```
run with qemu or similar e.g. ```qemu-system-x86_64 -fda tetros.bin```

enjoy!
